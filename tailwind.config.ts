import type { Config } from "tailwindcss";

const config: Config = {
	// content: [
	// ....
	// ],
	theme: {
		fontFamily: {
            anton: ['var(--font-anton)']

		},
	},
	plugins: [],
};
export default config;